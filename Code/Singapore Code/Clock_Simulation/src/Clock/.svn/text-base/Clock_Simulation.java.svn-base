package Clock;

import java.awt.*;
import javax.swing.*;

public class Clock_Simulation extends JFrame {

    public static JFrame jf = new JFrame("Clock_Simu");
    public static Clock_Simulation cs = new Clock_Simulation();
    public static Clock clk = new Clock(4,4,4,8);
    //public static Clock clk = new Clock(1);
    //hour,minute,second and speed;hour,minute and second are optional, but speed obligatory

    public static void main(String[] args){

        Container con = jf.getContentPane();
        con.setLayout(new FlowLayout());
        con.add(clk);

        jf.setTitle("Clock Simulation");
        jf.setVisible(true);
        jf.setSize(200,250);
        jf.setDefaultCloseOperation(EXIT_ON_CLOSE);
    }

}
